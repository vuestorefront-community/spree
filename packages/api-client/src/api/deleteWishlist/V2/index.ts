import { ApiContext, DeleteWishlistParams } from '../../../types';

export default async function deleteWishlistV2({ client, config }: ApiContext, { wishlistToken }: DeleteWishlistParams): Promise<void> {}
