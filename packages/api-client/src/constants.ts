/**
 * Default name of the cookie storing active, independent currency code for querying backend with (cart price computation, product prices, etc...).
 * Not to be confused with VSF_CURRENCY_COOKIE which is used for visual representation only and changes based on the active locale.
 */
export const VSF_SPREE_CURRENCY_COOKIE = 'vsf-spree-currency';
